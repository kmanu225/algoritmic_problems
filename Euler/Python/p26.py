from apremier import isprime


def check_motif(n):
    ch = str(n)[2::]
    motif = ch[0]


for d in range(1, 1000):
    # print(str(1/d)[2:8:])
    if len(str(1 / d)) > 9:

        f = 1 / d
        print(f"1/{d} = {f}", str(1 / d)[2:10:])

# if "ab" in "abcf" :
#     print("ok")
